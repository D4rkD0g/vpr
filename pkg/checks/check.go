// Package checks defines the interface for performing checks or assertions within a PoC step
// and provides a registry for different check implementations (e.g., HTTP status, body content).
// Each specific check type should implement the Check interface and register itself.
package checks

// TODO: Define Check interface and registry
